# Exercício 04

Exercício de React com build usando esbuild.

## Como executar

1. Instale dependências: `npm install`
2. Execute em modo desenvolvimento: `npm start`
3. Para build: `npm run build`
4. Abra `index.html` no navegador.